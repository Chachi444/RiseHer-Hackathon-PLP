[build]
  command = "npm run build"
  publish = "dist"

# optional: set Node version for Netlify build environment (match your local node)
[context.production.environment]
  NODE_VERSION = "18"

# optional: directory for Netlify Functions (remove if unused)
# functions = "netlify/functions"
